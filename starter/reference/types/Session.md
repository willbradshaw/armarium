Pages with `type: "[[reference/types/Session]]"` are play-session records containing preparation and actual play notes, including events, interactions, and rewards.

Use [[reference/templates/Session]] for the page structure.
